# SMS Forwarder 产品需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | SMS Forwarder |
| 文档版本 | v0.79 |
| 文档状态 | 开发基线 |
| 目标平台 | iOS 快捷指令 + 自托管 Web 收件箱（Android 原生端规划中） |
| 首版目标渠道 | HTTPS Webhook + Web 控制台 |
| 更新日期 | 2026-08-10 |

> v0.79 解除验证码次数限制：公开接码链接在有效期内不再限制接收验证码次数；后续新码持续覆盖最新验证码，接口仅返回最近 3 条用于展示，号码占用仍只在链接过期、管理员撤销或重置后释放。
>
> v0.78 号码占用释放：公开接码链接收到第 2 次验证码后仅停止接收并标记完成，但号码占用继续保留到该链接过期；管理员撤销或重置仍可强制结束占用。
>
> v0.77 验证码次数限制：每个公开接码链接最多接收 2 次验证码；收到第 2 次验证码后任务标记完成并停止接收后续新码。已有活动任务如已达到 2 条验证码，会在迁移后标记完成。
>
> v0.76 验证码窗口续收：公开接码任务不再因收到 3 条验证码而结束；任务在到期或撤销前持续接收新验证码，仅保留最近 3 条不重复验证码用于公开状态，已因旧规则完成但尚未过期的任务会恢复为活动状态。
>
> v0.75 同号码验证码共享：同一接收号码下多个仍在有效期内的公开接码任务共享新验证码；一条短信可路由到所有匹配号码、内置规则和有效时间窗口的任务，每个任务仍保留独立过期时间、最近 3 条不重复验证码和最新验证码展示。
>
> v0.74 公开链接隔离：公开领取页在 URL Fragment 存在 Token 时，后续领取和状态轮询继续带 Bearer 头；服务端优先按该 Token 定位链接，避免同一浏览器短时间打开多个卡密时被全局公开 Cookie 会话串页，Cookie 仍作为无 Fragment Token 的兜底会话。
>
> v0.73 密钥复制接口：`/api/share-links/copy` 返回字段改为 `content` 链接数组和 `count`，不再返回 `text` 字符串；管理端一键复制时继续用换行符拼接。
>
> v0.72 密钥只读接口：密钥列表和复制接口增加 `validity_hours=12|24` 筛选，并允许使用当前 Webhook Bearer Token 做只读鉴权；生成、撤销和号池管理仍要求管理员会话。
>
> v0.71 密钥复制格式：密钥页“一键复制”当前筛选下全部完整领取链接时，多个链接改为使用换行符分隔，不再使用制表符。
>
> v0.70 公开验证码状态：公开领取页改为仅显示最新一条验证码，新码覆盖旧码；收到后显示醒目的 `NEW` 状态和 60 秒倒计时，超过 60 秒后状态切换为“已过期”，验证码区域保持白色背景并继续等待下一条新码覆盖。
>
> v0.69 接入基线：确认 iOS 快捷指令可从新短信自动化取得正文，并通过带 Bearer Token 的 JSON POST 请求接入 Webhook v1；同步明确最小请求体、隐私边界、设备验证要求，以及 Android 原生端尚未实现的状态。
>
> v0.68 安全清理：移除前端已经停用的独立使用记录接口及对应数据库查询，清理未使用变量、参数和导入；未知 `/api/*` 路径统一返回 404，不再回落到管理端 HTML。
>
> v0.67 密钥列表间距：桌面端与平板端将“有效期”和“状态”两列的表头及内容统一居中，平衡完整领取链接、复制按钮与后两列之间的可见间距；手机端继续左对齐。
>
> v0.66 安全加固：升级 FastAPI、Starlette、cryptography 与 Uvicorn 安全基线；增加 Host 白名单、64 KiB 请求体上限、严格 Bearer 解析、跨源退出保护、HSTS/CSP/跨源隔离响应头、限流器容量上限、数据库文件 0600 权限，以及只读容器根文件系统和进程数限制。
>
> v0.65 教程辅助文字：将“温馨提示”标签及三个教程步骤的编号、说明文字统一放大至 13px，提升桌面端与手机端可读性。
>
> v0.64 登录步骤提示：提示正文改为“请先参考教程完成验证码发送，再耐心等待上方显示验证码。”，字号由 14px 提升至 15px。
>
> v0.63 登录步骤提示：取消可见的“手机号登录教程”标题，将“温馨提示”信息条独立放置在教程区域左侧，并将提示正文放大至 14px；教程区域保留无障碍名称。
>
> v0.62 登录教程提示：教程标题旁增加礼貌的“温馨提示”信息条，引导用户先完成验证码发送，再耐心等待上方显示验证码；桌面端与标题并排，手机端移至标题下方。
>
> v0.61 成功区域布局：桌面端“成功接收”徽标与验证码框按垂直中心对齐；移除状态单元格整块绿色底色，恢复白色外部背景，仅在成功徽标、验证码框与复制按钮上保留绿色反馈。
>
> v0.60 手机端验证码对齐：将“短信验证码：”标签设置为与首条验证码相同的 44px 行高并垂直居中，多条验证码时持续对齐第一条验证码。
>
> v0.59 验证码成功反馈：领取页收到首条验证码后立即将状态改为“成功接收”，状态徽标、状态区域、验证码与复制按钮统一使用绿色成功样式；任务继续轮询后续验证码，窗口到期后仍保留成功反馈。
>
> v0.58 手机端客服提示：顶部服务条改为左右两列布局，左侧两行展示祝福与客服说明，右侧 QQ 复制按钮跨两行居中，减少无效留白并避免典型 iPhone 宽度下出现三行。
>
> v0.57 领取页号码样式：号码字重由粗体调整为常规字重，移除号码牌左侧蓝色识别条，保留浅蓝底、完整细边框与原有尺寸。
>
> v0.56 领取页字号协调：顶部祝福、客服提示及 QQ 复制控件统一小幅放大；桌面表头“手机号码/状况/时间”与手机端对应标签同步提升字号，保持跨端层级一致。
>
> v0.55 领取页客服 QQ：关闭 iOS 自动电话号码识别，避免 QQ 号码变蓝并触发拨号；客服 QQ 改为一键复制控件，并复用 iOS 剪贴板兼容与手动选中兜底。
>
> v0.54 领取页号码样式：完整手机号码改为独立号码牌视觉，采用浅蓝底、蓝色识别边、等宽数字间距与统一高度的复制按钮，提升主信息的辨识度和精致感。
>
> v0.53 领取页服务提示：页面顶部增加礼貌的观影祝福与客服 QQ 信息，桌面端以单行服务条呈现，手机端自然换行并保持清晰层级。
>
> v0.52 领取页复制：兼容 iOS Safari、非安全上下文和受限内嵌浏览器；现代剪贴板不可用时回退到页面选区复制，仍受限时自动选中号码或验证码供长按复制。
>
> v0.51 密钥布局：每行复制按钮紧跟完整领取链接，桌面端间距为 8px；手机端复制按钮继续独占一行，保持足够触控区域。
>
> v0.50 密钥布局：状态筛选、一键复制及其下方内容整体上移 15px；桌面和中等宽度加大领取链接与有效期的列间距，手机端保持单列。
>
> v0.49 列表分页：看板使用记录、号池和密钥列表统一每页显示 20 条，超过一页时提供上一页、下一页和当前页码。
>
> v0.48 密钥管理：移除 CSV 导出，改为一键复制当前筛选条件下的全部完整领取链接。
>
> v0.47 密钥管理：支持按“待使用/已使用”状态筛选，并将当前筛选条件下的全部完整领取链接、有效期和状态导出为 CSV。
>
> v0.46 号池与密钥：号池新增使用次数重置操作，确认后结束该号码当前接码任务并将次数清零；密钥“已使用”状态改为红色标签。
>
> v0.45 使用记录：Key 列展示包含完整 Token 的领取链接，并支持按完整链接搜索；长链接在桌面端和移动端完整换行，不做省略。
>
> v0.44 数据保留：Web 收件箱移除“短信数据”设置、保留天数与一键清空；短信记录长期加密保存且不再自动过期清理，管理员仍可按需删除单条记录。
>
> v0.43 使用记录：已登录管理端展示真实完整 Key 与完整接收手机号；宽屏使用四列表格，中等宽度使用分层卡片，手机端使用单列字段布局。
>
> v0.42 使用记录：看板列表统一展示 Key 编号、脱敏手机号、短信摘要和接收时间；未关联公开接码任务的短信以“—”表示 Key。
>
> v0.41 看板指标：新增“可用次数”，按所有已启用号码的剩余分配次数之和计算；“可用号码”继续表示尚有分配余量的号码数量。
>
> v0.40 看板精简：原“短信 / 使用记录”双标签合并为单一“使用记录”区域，保留短信摘要搜索、详情与分页能力，不再展示独立任务记录标签页。
>
> v0.39 登录页精简：移除欢迎语与说明文案，应用图标和“SMS Forwarder”品牌名在登录表单上方同行展示。
>
> v0.38 移动端适配：管理端采用顶部账户栏与底部固定四模块导航，列表在窄屏显示字段标签并扩大触控区域；公开领取页优化号码、状况、验证码和倒计时的单列布局，并适配安全区与移动端输入体验。
>
> v0.37 界面调整：公开领取页教程标题改为“手机号登录教程”，并缩小字号以降低视觉层级。
>
> v0.36 视觉调整：公开领取页与管理端统一文字色彩层级，采用深海军蓝主文字、中性灰辅助文字、低饱和蓝操作色和沉稳绿状态色，提高可读性并减少突兀感。
>
> v0.35 界面调整：公开领取页“状况”列参考状态样式，将状态胶囊与“短信验证码：”标签并列；等待时冒号后留空，收到后在该位置展示验证码。
>
> v0.34 界面调整：公开领取页“状况”列移除“短信验证码 0/3”计数，只保留状态提示和实际验证码。
>
> v0.33 界面调整：公开领取页移除独立验证码列，收敛为“手机号码、状况、时间”三列；验证码计数、等待提示、验证码内容和复制操作统一放入“状况”列。
>
> v0.32 界面调整：侧栏账户入口移除圆形字母头像，仅保留管理员名称、角色和展开箭头。
>
> v0.31 分配调整：号码当前使用次数未达到分配上限时，可继续分配给新的接码任务；达到上限后才退出可用库存。
>
> v0.30 品牌调整：登录页、管理侧栏、管理页浏览器标签和公开领取页浏览器标签统一使用绿色短信气泡图标。
>
> v0.29 界面调整：公开领取页将“状态”调整为“状况”，等待阶段使用带圆点的深色描边状态胶囊，并在同一区域展示短信验证码计数。
>
> v0.28 行为调整：公开领取页成功验证分享 Token 后保留地址栏中的 `#t=...` 后缀，不再改写为 `/c`。
>
> v0.27 界面调整：公开领取页移除顶部状态栏，并统一手机号、验证码和倒计时的数字字体、字重与字距。
>
> v0.26 界面调整：公开领取页的接码信息采用“手机号码、验证码、状态、时间”表格式布局，不展示成本，倒计时固定放在最后一列。
>
> v0.25 界面调整：公开领取页精简为号码、倒计时和验证码的单一信息卡，并新增腾讯视频手机号登录三步图片教程；教程素材不得包含真实完整手机号。
>
> v0.24 接口调整：Web 收件箱的 Webhook 请求体仅要求 `message` 和 `recipient`；投递 ID、发送方和接收时间缺省时由服务端生成。
>
> v0.23 功能调整：设置页支持生成并轮换 Webhook Token；新 Token 仅在生成时完整显示并支持复制，服务端只保存摘要，旧 Token 立即失效。
>
> v0.22 界面调整：四个管理模块使用独立 URL 锚点；模块切换、刷新、浏览器前进后退和链接分享均保留当前模块。
>
> v0.21 行为调整：号池“使用次数”表示号码当前被有效链接占用的次数；新号码从 0 开始，链接领取成功后增加，链接到期、完成或撤销后释放并减少。
>
> v0.20 界面调整：密钥生成后不再额外展示“生成成功”结果面板；新链接直接进入密钥历史列表，并保留单条复制。
>
> v0.19 界面调整：侧栏底部默认显示当前管理员账户入口，点击后才展开“退出登录”；点击外部或按 Esc 收起。
>
> v0.18 界面调整：密钥历史展示完整领取链接，每条链接支持一键复制。
>
> v0.17 界面调整：管理端模块名称进一步精简，“号码池”统一改为“号池”，“密钥工厂”统一改为“密钥”。
>
> v0.16 范围变更：新密钥使用 Fernet 加密保存原始值，管理端历史列表直接显示完整密钥；摘要继续用于公开验证。旧密钥因此前未保存原始值而不可恢复。
>
> v0.15 界面调整：密钥历史仅展示密钥、有效期和状态，移除筛选、勾选、批量撤销和操作列；公开状态统一为“待使用”和“已使用”两种。
>
> v0.14 范围变更：新密钥的有效期从首次打开且成功分配号码后开始，仅支持 12 小时或 24 小时；未打开或无库存时不开始倒计时，买家页使用时分秒倒计时。
>
> v0.13 范围变更：密钥取消领取截止时间，生成后在使用或撤销前持续有效；密钥表不再保存号码或任务外键，号码只在首次访问时分配给独立接码任务。密钥只展示密钥、有效期、状态和操作。
>
> v0.12 界面调整：号池的号码、使用次数、状态和操作四列统一为等宽布局，表头、数据行和行内编辑保持对齐。
>
> v0.11 界面调整：号池仅展示号码、使用次数、状态和操作；地区信息及可由次数推导的剩余次数不再展示，新增时只需填写号码和分配上限，新号码默认中国大陆。
>
> v0.10 界面调整：管理端统一提升基础字号以及导航、表格、表单、按钮和状态标签字号，改善桌面端与移动端的可读性。
>
> v0.9 范围变更：号池行内新增与编辑不再要求填写地区代码；新增号码内部默认使用 `CN`，已有号码编辑时保留原地区代码，界面只显示地区名称。

## 2. 背景与问题

用户可能需要把特定短信同步到自有系统，例如接收设备告警、订单状态、值班通知或个人自动化事件。当前可行路径是在 iOS 中由用户明确创建短信触发的快捷指令自动化，把新短信正文和接收号码提交到自托管服务；人工复制容易遗漏，而直接读取系统短信数据库既不必要也不符合最小权限原则。

SMS Forwarder 需要提供范围清晰的接入方式：由用户在设备端决定哪些新短信触发快捷指令，只发送到自己控制的 HTTPS Webhook，并能在自托管管理端确认接收结果。

## 3. 产品目标

### 3.1 首版目标

- 用户可按文档在 iOS 快捷指令中完成短信触发器、Webhook 地址、Bearer Token 和最小 JSON 请求体配置。
- 符合快捷指令触发条件的新短信可在正常网络下及时投递到自托管 Webhook。
- 服务端对稳定 `delivery_id` 执行幂等去重；快捷指令未提供时由服务端生成随机 ID。
- 用户能在管理端查看 Webhook 地址、轮换 Token，并确认最近接收结果。
- 短信正文、手机号和 Webhook 凭据得到最小化处理与安全保护。
- 用户可将 Webhook 指向自托管服务，并在登录后查看已转发的短信。
- 管理员可通过 Key 为访问者开启限时接码任务，并只展示全局规则提取出的验证码。

### 3.2 非目标

- 不读取或批量上传安装前的历史短信。
- 不提供短信发送、自动回复、验证码自动填写或群发功能。
- 不提供原生 iOS 客户端，不读取 iOS 短信数据库；当前 iOS 支持仅指用户主动配置的系统快捷指令自动化。
- 首版不提供多租户云端账户、跨设备规则同步或团队协作。
- 首版不支持邮件、聊天机器人等额外投递渠道。
- 不接入闲鱼等交易平台的订单、支付、自动发货或页面抓取能力。
- 不提供批量注册、验证码自动填写、绕过第三方身份验证或违反第三方服务条款的用途。

## 4. 目标用户与场景

### 4.1 目标用户

- 希望将个人设备中的特定通知接入自有自动化流程的技术用户。
- 使用专用 SIM 卡接收设备、业务或值班告警的小型团队管理员。
- 重视本地处理、可控数据范围和透明投递状态的隐私敏感用户。

### 4.2 核心场景

1. iPhone 用户为指定发件人或关键词创建短信自动化，仅将命中的新短信发送到自有服务。
2. 快捷指令向固定 Webhook 提交短信正文和该自动化对应的接收号码。
3. 管理员轮换 Webhook Token 后同步更新快捷指令，旧 Token 立即失效。
4. 管理员在 Web 控制台查看使用记录，并确认短信是否进入有效接码任务。
5. 用户需要停止转发时，停用或删除对应的快捷指令自动化。

## 5. 核心概念

| 概念 | 定义 |
| --- | --- |
| 短信事件 | 设备新收到的一条短信及其必要元数据 |
| iOS 快捷指令发送端 | 用户主动配置的短信自动化，从触发事件取得正文并向自托管 Webhook 发送 POST 请求 |
| 规则 | 由匹配条件、目标 Webhook、发送字段和启停状态组成的本地配置 |
| 投递任务 | 一条短信命中一条规则后生成的可追踪发送任务 |
| 投递 ID | 每个投递任务稳定且唯一的标识，用于追踪和幂等 |
| 转发记录 | 仅用于展示投递状态的本地记录，默认不长期保留完整正文 |
| 内置短信规则 | 不限制发件人，固定提取独立的 4 至 8 位数字验证码 |
| 号码库存 | 管理员合法持有的自有 SIM 号码及其可分配次数 |
| 分享链接 | 带高熵随机凭证和任务有效期的公开访问入口，首次使用或撤销前持续有效 |
| 接码任务 | 分享链接首次访问后独立创建并绑定固定号码的一次限时接收窗口 |

## 6. 首版用户流程

1. 部署者完成自托管服务的 HTTPS 配置，在管理端取得 Webhook 地址和独立 Bearer Token。
2. iPhone 用户在“快捷指令 → 自动化”创建短信触发器，并限制发件人或关键词范围。
3. 用户配置“获取 URL 内容”为 JSON POST，请求体把快捷指令输入映射为 `message`，并为该自动化填写完整 `recipient`。
4. 新短信命中触发器后，快捷指令向自托管 Webhook 发送请求；服务端验证 Token、字段与请求大小。
5. 服务端补齐缺省投递 ID、发送方和 UTC 时间，按投递 ID 去重并加密保存短信。
6. 用户登录 Web 控制台查看、搜索或删除已接收的短信。
7. 管理员配置 12 小时或 24 小时的默认有效期和自有号码，为访问者批量生成 Key。
8. 访问者打开未使用且未撤销的链接；系统自动原子创建接码任务并分配一个号码，在整个窗口内保持固定。
9. 新短信同时匹配接收号码、接码窗口和内置验证码规则时，公开页只展示最新验证码；新码覆盖旧码。
10. 窗口在有效期内不限制接收验证码次数，公开页只展示最新验证码并返回最近 3 条记录；超时或管理员撤销后结束。

## 7. 功能需求

### 7.1 当前已实现：iOS 快捷指令接入

#### FR-00 iOS 快捷指令发送端

- 用户必须主动创建并启用短信触发的个人自动化；项目不读取 iOS 短信数据库，也不扫描历史短信。
- 触发器应按发件人或关键词尽量收窄，停用或删除自动化后不得继续发送新事件。
- 快捷指令必须向管理端提供的 HTTPS `/api/webhooks/sms` 地址发送 JSON POST，并设置 `Authorization: Bearer <WEBHOOK_TOKEN>` 与 `Content-Type: application/json`。
- 最小请求体只包含非空的短信正文 `message` 和完整接收号码 `recipient`。可选字段包括 `delivery_id`、`sender`、`received_at` 等 Webhook v1 字段。
- 快捷指令未提供 `delivery_id` 时服务端生成随机值；需要重试的自动化应自行生成并复用稳定 ID，避免重复保存。
- Token 不得出现在共享快捷指令、截图、普通日志或公开文档中；泄露后管理员必须立即轮换 Token 并更新快捷指令。
- iOS 是否要求运行确认、能否锁屏执行由系统版本和自动化设置决定，必须在目标真机上验证，不能由服务端测试替代。

### 7.2 规划：Android 原生客户端（尚未实现）

#### FR-01 首次启动与权限

- 首次启动必须在请求系统权限前说明权限用途、处理的数据和风险。
- 用户拒绝权限后，应用仍可进入配置界面，但必须明确显示无法自动转发。
- 应用必须能识别权限被系统撤销，并停止接收及转发新短信。
- 不得请求通讯录、位置、相机等与核心功能无关的权限。

#### FR-02 总开关

- 首页必须提供全局转发开关，并清晰显示当前状态。
- 关闭总开关后，不再为新短信创建投递任务。
- 已在发送中的请求允许结束；待重试任务暂停，重新开启后恢复。

#### FR-03 规则管理

- 用户可创建、查看、编辑、启停和删除规则。
- 每条规则至少包含：名称、发件人条件、正文关键词条件、Webhook 地址和启停状态。
- 发件人条件支持精确匹配；正文条件支持“包含任一关键词”。
- 条件留空表示不限制该字段，但发件人与正文条件不得同时为空，防止误转发全部短信。
- 多条规则可命中同一短信，并分别生成投递任务。
- 规则列表展示名称、启停状态、脱敏目标主机和最近一次结果。

#### FR-04 Webhook 配置与测试

- Webhook 只允许使用有效的 `https://` 地址。
- 用户可配置请求头；敏感请求头的值默认遮盖显示，并存入平台安全存储。
- “测试连接”使用示例数据，发送前必须明确标识为测试事件。
- 连接测试展示成功、超时、网络错误或服务端状态码，不展示敏感响应正文。
- 默认连接超时 10 秒、读取超时 20 秒；具体值可在技术设计阶段调整并记录。

#### FR-05 匹配与投递

- 仅处理规则启用期间新收到的短信，不自动扫描历史短信。
- 同一短信分段到达时，应尽可能合并为一条完整事件后再匹配。
- 对发件人做一致的格式标准化，但保留原始值用于用户选择的发送字段。
- 每个投递任务生成稳定的投递 ID；重试沿用同一 ID。
- Webhook 请求体至少包含以下字段：

```json
{
  "version": "1",
  "event": "sms.received",
  "delivery_id": "唯一投递 ID",
  "rule_id": "命中的规则 ID",
  "sender": "发件人",
  "recipient": "接收号码",
  "message": "短信正文",
  "received_at": "ISO 8601 UTC 时间",
  "is_test": false
}
```

- 请求头必须包含 `Authorization: Bearer <WEBHOOK_TOKEN>` 和 `Content-Type: application/json`。
- 任意 `2xx` 响应视为成功；响应正文不得默认写入日志。

#### FR-06 失败处理与重试

- 断网、超时、`408`、`429` 和 `5xx` 可自动重试。
- 其他 `4xx` 默认视为永久失败，等待用户修正配置后手动重试。
- 自动重试采用退避策略，首版建议为 1 分钟、5 分钟、30 分钟、2 小时、6 小时，共 5 次。
- `429` 响应存在有效 `Retry-After` 时优先遵循服务端建议，但不得无限延迟。
- 设备或应用重启后，未完成任务应可恢复。
- 达到重试上限后标记为失败，不得无限后台唤醒设备。

#### FR-07 转发记录

- 记录页按时间倒序展示任务状态、规则名称、脱敏发件人、创建时间、尝试次数和失败原因。
- 状态至少包括：待发送、发送中、等待重试、成功、失败、已取消。
- 默认不在记录列表显示完整短信正文。
- 用户可删除单条记录或清空全部记录。
- 本地记录默认保留 7 天，过期后自动删除；该期限在设置中可调整为 1、7 或 30 天。

#### FR-08 隐私与数据控制

- 规则和发送任务仅保存在设备本地；用户主动转发到自托管 Webhook 的短信记录保存在其服务器。
- Webhook 凭据使用目标平台提供的安全存储能力保护；Android 具体方案需在客户端实现前评审。
- 应用不得接入会采集短信内容或完整手机号的分析、广告及崩溃报告服务。
- 诊断日志中的手机号仅保留末 4 位，正文不得写入普通日志。
- 用户可在设置中清除全部规则、凭据、任务和记录。
- 卸载应用后，本地数据应随应用数据一并删除，不主动保留云端副本。

### 7.3 当前已实现：自托管 Web 收件箱与限时接码

#### FR-09 自托管 Web 收件箱

- Webhook 接收端必须使用与管理端密码不同的 Bearer Token 验证请求。
- 使用 `delivery_id` 做幂等去重，相同投递 ID 不得产生重复短信记录。
- 发件人和短信正文必须加密后持久化；普通日志不得记录请求正文、令牌或完整手机号。
- 管理页面必须经过登录会话保护，支持短信列表、正文搜索、详情和单条删除，不提供一键清空。
- 短信记录长期加密保存，不提供保留期配置，也不执行自动过期清理。
- 采用单租户自托管模式，不提供公开注册、第三方分析或运营人员入口。
- 公网入口只允许 HTTPS；本机开发可使用 localhost HTTP。
- Webhook 必须兼容 iOS 快捷指令发送的最小 JSON；合法 Token 且仅含 `message`、`recipient` 时应成功接收。

#### FR-10 四模块限时号码领取

- Web 管理端统一使用中文，仅保留“看板”“号池”“密钥”“设置”四个导航，分别使用 `#dashboard`、`#numbers`、`#keys`、`#settings` 锚点，仍采用单管理员、单租户模式。
- 看板显示可用号码、可用次数、接码中、待使用密钥和今日短信五个指标；使用记录分页展示短信摘要，每页 20 条。
- 使用记录通过受管理员会话保护的接口返回含真实完整 Key 的领取链接、完整接收手机号、时间与截断短信摘要，并支持按完整领取链接搜索；未关联公开接码任务时 Key 显示“—”。完整短信正文仍须通过受管理员认证保护的单条详情接口按需读取，普通日志不得记录 Key、完整号码或正文。
- 号池中的号码必须加密保存，并使用不可逆指纹精确匹配；字段只包含号码、地区、分配上限、当前使用次数和启停状态，不提供永久删除。号池列表每页显示 20 条并提供分页。新号码的当前使用次数为 0；每行提供“重置次数”操作，管理员确认后结束该号码全部活动任务并将当前使用次数清零，历史短信和验证码继续保留。
- 分配不限制地区，从全部可用号码中按当前使用次数最少、ID 最小选择；同一时间一个号码最多存在“分配上限”个未过期任务。链接成功领取号码后当前使用次数增加 1；链接到期、管理员撤销或重置后当前使用次数减少 1，且不得低于 0。当前使用次数达到分配上限后才退出可用库存。
- 密钥每批原子生成 1 至 20 个密钥，共用 12 小时或 24 小时有效期；生成后直接进入密钥历史列表并支持单条复制，不提供额外结果面板或轮换。
- 分享凭证使用高熵随机值，数据库保存验证摘要和 Fernet 加密原始值；管理端历史接口可解密显示完整密钥，公开验证只使用摘要。密钥在首次使用或撤销前持续有效，成功创建接码任务后才按所选有效期开始倒计时。
- 公开页保留 Fragment Token 作为当前标签页的链接选择器；当请求携带公开 Bearer Token 时，`/api/public/claim` 和 `/api/public/state` 必须优先按 Token 定位链接，避免多个领取页共享同一浏览器 Cookie 时互相覆盖；没有 Token 的请求继续使用已交换的短期 HttpOnly 会话。
- 密钥表不保存号码或任务外键；打开密钥后自动、幂等地创建独立接码任务并分配固定号码。无库存时不得创建任务或扣减号码次数，密钥保持未使用。
- 密钥历史每页 20 条，仅展示完整领取链接、有效期和状态，每条链接支持单条复制；支持按“待使用/已使用”状态筛选，并一键复制当前筛选条件下不受分页限制的全部完整领取链接，多个链接使用换行符分隔。密钥列表和复制接口额外支持 `validity_hours=12|24` 查询参数，并可由管理员会话访问，也可由当前 Webhook Bearer Token 访问作为只读自动化接口；复制接口返回 `content` 链接数组和 `count`，不返回 `text` 字符串。生成、撤销、号池和设置接口仍只能由管理员会话访问。未创建接码任务且未撤销时为“待使用”，其余统一显示“已使用”，“已使用”使用红色状态标签。
- 设置模块按接码设置、Webhook 接入和账户安全分类；提供 12/24 小时默认有效期、Webhook 说明、Token 生成轮换和改密表单，不提供正则编辑、短信保留期或一键清空入口。新 Webhook Token 仅在生成时完整返回一次，数据库只保存验证摘要，轮换后旧 Token 立即失效。
- Web 收件箱的 Webhook 请求体仅要求短信正文 `message` 和接收号码 `recipient`。缺省 `delivery_id` 时服务端生成随机投递 ID，缺省 `sender` 时记为 `Webhook`，缺省 `received_at` 时使用当前 UTC 时间；调用方仍可传入稳定 `delivery_id` 获得重试幂等能力。
- 公开路由必须同时匹配接收号码、内置验证码规则和有效时间窗口；同一接收号码下所有匹配的有效接收任务都应收到新验证码，不设置接收次数上限；公开状态返回实际去重验证码数量、最近 3 条验证码和最新验证码及其 60 秒展示有效期，号码占用必须保留到该链接到期或被管理员撤销/重置。
- 公开页的接码信息采用“手机号码、状况、时间”三列表格式布局，仅显示完整领取号码、复制按钮、最新验证码、接码状况和倒计时；不设置独立验证码列，状态胶囊与“短信验证码：”标签并列，等待时冒号后留空，收到后在该位置展示最新验证码和复制操作，且不显示验证码数量计数。新的验证码必须覆盖旧验证码；收到后状态胶囊显示醒目的 `NEW` 并展示 60 秒倒计时，超过 60 秒后切换为“已过期”，验证码区域保持白色背景。不显示成本、服务、地区、完整短信正文或发件人，倒计时固定放在最后一列。页面下方提供腾讯视频手机号登录三步图片教程，教程素材不得包含真实完整手机号；响应不得缓存。
- 管理员密码使用带随机盐的 scrypt 哈希保存；改密必须验证当前密码、提升认证版本、注销其他会话并续签当前会话。环境变量只初始化新数据库，并提供不暴露密码的交互式本地恢复命令。
- 任务结束后号码若仍有剩余分配次数立即复用；界面必须明确提示迟到短信可能串单的风险。

### 7.4 P1：当前版本后评估

- 规则支持正则表达式、排除词、时间段和 SIM 卡槽条件。
- 发送字段可按规则选择，并支持正文局部遮盖。
- Webhook HMAC 签名与时间戳，便于接收方验证来源及防重放。
- 导入、导出经过加密的规则配置。
- 电池优化、后台限制和厂商兼容性的诊断页。
- 失败通知及每日投递摘要。

### 7.5 P2：暂不承诺

- 邮件、Telegram、Slack、企业微信等渠道。
- 多租户云端中继、远程规则管理和多设备规则同步。
- OCR、多媒体消息（MMS）或富媒体附件转发。
- 基于机器学习的短信分类或摘要。

## 8. 业务规则

### 8.1 当前 iOS 快捷指令与服务端

- iOS 快捷指令只处理用户配置后新触发的短信，不读取历史记录；设备端触发条件决定哪些短信会被提交。
- 快捷指令最小接入可省略投递 ID；一旦配置自动重试，就必须在各次尝试中复用同一 `delivery_id`。
- Webhook Token 轮换后旧值立即失效，所有使用该 Token 的快捷指令都必须同步更新。

### 8.2 Android 原生客户端规划

- 全局开关、系统权限和规则开关同时有效时，规则才可处理新短信。
- 同一短信命中多条规则时，每条规则独立投递，彼此失败不相互影响。
- 编辑规则只影响编辑完成后创建的新任务；已有任务沿用创建时的目标快照。
- 删除规则时，必须让用户选择取消其未完成任务或保留任务继续发送，默认取消。
- 手动重试沿用原投递 ID，并记录新的尝试次数。
- 规则名称在本地可重复，但界面必须同时展示目标主机以便区分。

## 9. 非功能需求

### 9.1 可靠性

- 正常网络和系统允许后台运行的情况下，95% 的命中短信应在收到后 30 秒内发起首次请求。
- 应用进程被系统终止后，已持久化的待处理任务不得永久丢失。
- 相同投递任务的并发发送数必须为 1，避免本地并发重复。

### 9.2 性能与资源

- 单条短信的本地规则匹配目标耗时低于 100 毫秒（100 条简单规则以内）。
- 无短信和无待处理任务时不得持续轮询或持有不必要的唤醒锁。
- 记录清理和重试调度应使用系统推荐的后台任务机制。

### 9.3 安全

- 所有外发请求必须使用 TLS，并依赖系统证书校验。
- 应防止凭据出现在通知、剪贴板、日志、崩溃信息和应用切换预览中。
- 导出诊断信息前必须展示将导出的字段，且默认不包含短信正文和请求头值。
- 依赖项需经过许可证、已知漏洞和维护状态检查。

### 9.4 可用性与兼容性

- 界面应提供简体中文；后续可扩展英文。
- 权限缺失、后台受限和网络不可用必须有可识别且可操作的状态提示。
- iOS 快捷指令操作名称和自动化确认策略可能随系统版本变化，README 使用通用步骤并要求目标真机验证。
- 最低 Android 版本、目标 SDK 和厂商兼容范围由未来原生客户端技术调研确定。
- Web 管理端和公开页的无障碍标签、触控区域与文本缩放应符合主流桌面及移动浏览器要求。

## 10. 页面范围

| 页面 | 主要内容 |
| --- | --- |
| iOS 快捷指令（系统界面） | 短信触发条件、Webhook 地址、Bearer Token、JSON 请求体 |
| 看板 | 五项指标、统一使用记录、搜索、分页和按需短信详情 |
| 号池 | 加密号码、地区、最大/已分配次数、编辑和启停 |
| 密钥 | 1–20 个批量密钥、12/24 小时有效期、生成后复制和三列历史列表 |
| 设置 | 默认有效期、Webhook 说明、Token 轮换和改密 |
| 公开领取页 | 固定号码、复制按钮、倒计时、最新验证码、60 秒 NEW/已过期状态和腾讯视频手机号登录三步图片教程 |

## 11. 数据保留与状态

- iOS 快捷指令配置与 Token：由系统快捷指令保存，停用或删除自动化后停止处理新短信；Token 泄露时由管理端轮换。
- Android 原生端的规则、离线任务和本地记录保留策略尚未实现，需在客户端开发前评审。
- Web 收件箱记录：在自托管服务器长期加密保存，不自动过期且不提供一键清空；管理员可按需删除单条记录。加密密钥由部署者独立保管，部署者需监控存储容量并妥善保护备份。
- 号码配置：保留至管理员停用，不提供物理删除；地区只用于库存管理，不限制分配。
- 分享凭证：验证摘要和 Fernet 加密原始 Token 持久化；完整值只通过已认证且禁止缓存的管理接口返回，不写入日志。
- 公开验证码：依附于加密短信记录，随关联短信单条删除；公开页只突出显示最新一条验证码及其 60 秒状态，完整短信仅管理员可见。

## 12. 成功指标

首版不依赖采集短信内容的云端埋点。若发布渠道允许，可优先使用不含个人数据的本地统计并由用户主动导出。

- iOS 快捷指令测试短信到后台成功落库的完成率。
- Webhook 首次接收成功率及带稳定投递 ID 的幂等命中率。
- 因快捷指令确认、网络、Token 失效或字段错误导致的失败占比。
- 公开接码任务的验证码正确路由率与重复验证码拦截率。

指标采集方案上线前必须单独评审，默认不启用远程分析。

## 13. 验收标准

当前已实现范围至少满足：

1. README 给出 iOS 快捷指令的短信触发器、HTTPS URL、请求方法、认证头和最小 JSON 配置，并提示真机验证与 Token 风险。
2. Web 收件箱仅凭合法 Token、`message` 和 `recipient` 即可接收事件；缺少任一必填字段时返回 `422`。
3. 无效、格式错误或已轮换失效的 Bearer Token 被拒绝，Token 和请求正文不进入普通日志。
4. 缺省 `delivery_id`、`sender`、`received_at` 时服务端生成安全默认值；提供相同稳定投递 ID 时返回成功但不新增记录。
5. 未登录用户无法读取或删除 Web 收件箱数据，数据库中不存在明文短信正文、发件人和接收号码。
6. 分享 Token 摘要用于公开验证，完整值不进入访问日志；过期、撤销或伪造 Token 均被拒绝。
7. 并发或重复申请同一链接只产生一个接码任务，并始终返回同一号码。
8. 无库存时不增加号码分配次数；达到号码最大分配次数后不再进入库存。
9. 公开页只返回匹配当前任务的最近 3 条验证码、实际验证码数量和最新验证码状态，不返回完整正文、完整发件人或其他任务数据；界面只显示最新验证码，新码覆盖旧码，60 秒后状态切换为“已过期”；接收次数不设上限。
10. 数据库旧版本升级后历史短信可正常读取，且不会被错误公开路由。
11. 管理页面和公开领取页在桌面及手机浏览器保持可用，敏感响应禁止缓存。
12. 服务端核心鉴权、字段校验、幂等、加密、路由、并发和迁移场景均有自动化测试。

Android 原生客户端的权限、设备端规则、离线重试和进程恢复验收在对应实现进入仓库后再启用，不能以当前 iOS 快捷指令接入替代。

## 14. 风险与约束

| 风险 | 影响 | 应对方向 |
| --- | --- | --- |
| iOS 快捷指令因系统版本或自动化设置要求确认、锁屏不执行 | 短信转发延迟或漏发 | README 记录通用配置，目标设备真机验证，收窄触发条件并监控后台使用记录 |
| 快捷指令内的 Bearer Token 被截图或共享 | 未授权请求可写入后台 | 禁止共享敏感快捷指令，使用独立 Token，支持立即轮换并让旧值失效 |
| Android 权限及应用商店政策限制 | 可能无法上架或权限申请被拒 | 尽早确认分发渠道和政策资格，准备清晰的核心功能说明 |
| 厂商后台限制 | 短信处理或重试延迟 | 使用平台推荐机制，提供后台限制诊断与引导 |
| Webhook 重试造成重复业务 | 接收方执行重复操作 | 稳定投递 ID、文档化幂等要求，P1 增加签名 |
| 手机号和短信格式差异 | 规则误匹配或漏匹配 | 建立标准化层并用多地区样例测试 |
| 敏感数据泄露 | 严重隐私与安全问题 | 数据最小化、安全存储、日志审计和发布前安全测试 |
| 号码立即复用导致迟到验证码串单 | 前一用户的迟到短信可能被后一用户看到 | 公开提示风险、严格按接收时间路由；后续优先评估可配置隔离期 |
| 分享链接泄露 | 未授权用户可领取或查看验证码 | URL Fragment、Token 摘要、加密原始值、短期会话、限流和管理员撤销 |
| 设备时钟不准确 | 时间判断及签名异常 | 使用 UTC、记录设备时间来源，签名功能考虑容差 |

## 15. 待确认事项

- iOS 首发支持版本、短信自动化在锁屏与不同系统设置下的确认行为。
- Android 原生应用未来的实际分发渠道：Google Play、国内应用商店、企业分发或侧载。
- 首发覆盖的国家/地区、短信格式和双卡设备要求。
- 最低 Android 版本、UI 技术栈、持久化与后台任务方案。
- Webhook 是否需要首版即支持 HMAC 签名或其他鉴权模板。
- 是否允许用户选择不发送正文，或对正文进行字段提取与遮盖。
- 完整正文在待重试期间的加密存储方式及密钥轮换策略。
- 产品是否仅供个人使用，还是需要满足团队、审计或合规要求。
