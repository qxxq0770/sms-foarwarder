# AGENTS.md

本文档用于指导在本仓库中工作的开发者与 AI 代理。除非更深层目录存在单独的 `AGENTS.md`，本文件适用于整个仓库。

## 1. 项目概述

`sms-forwarder` 是一个单管理员、自托管的短信 Webhook 收件箱与限时接码服务。当前已经跑通的发送端是用户主动配置的 iOS 快捷指令自动化：快捷指令从新短信事件取得正文，向自托管 `/api/webhooks/sms` 接口发送带 Bearer Token 的 JSON POST 请求。Android 原生客户端仍处于规划阶段，仓库中尚无 Android 代码。

产品需求与验收标准见 [PRD.md](./PRD.md)。当实现与 PRD 冲突时，应先说明冲突并更新文档或实现，不得静默改变产品行为。

## 2. 当前状态

- Web 收件箱采用 Python 3.12、FastAPI、SQLite 和原生 HTML/CSS/JavaScript，使用 Docker Compose 部署。
- 服务代码位于 `app/`，自动化测试位于 `tests/`；iOS 侧使用系统快捷指令，不包含需要编译的客户端代码，Android 客户端仍未实现。
- Webhook v1 仅要求 `message` 和 `recipient`；服务端补齐投递 ID、发送方和 UTC 时间。快捷指令接入步骤与请求示例记录在 `README.md`。
- 本地测试命令为 `pytest`，容器构建命令为 `docker compose build`。
- 不得虚构不存在的脚本、依赖、接口或测试结果。
- 引入框架或第三方服务前，应说明选择理由、替代方案及维护成本。
- 优先完成可验证的最小闭环，避免提前加入多渠道、云端账户体系或远程管理能力。

## 3. 核心原则

### 3.1 隐私与安全优先

- 短信属于高度敏感数据，只能在用户明确创建并启用快捷指令自动化后处理；不得扫描或上传历史短信。
- 短信只能发送到用户控制的自托管服务。服务端按当前产品要求长期加密保存，普通日志、第三方分析和未加密备份不得包含正文。
- 日志、崩溃报告、埋点和测试快照不得包含完整手机号、短信正文、令牌或 Webhook 密钥。
- Webhook 仅允许使用 HTTPS；不得绕过证书校验。
- 凭据必须使用平台安全存储，不得明文写入源码、普通配置文件或版本库。
- UI 必须清楚展示将发送的字段、目标地址和启停状态。
- 新增权限时必须提供必要性说明，并遵循最小权限原则。

### 3.2 本地优先

- iOS 快捷指令的短信触发条件应尽量在设备端收窄；服务端只接收快捷指令明确提交的事件。
- 未经 PRD 明确批准，不得新增第三方中心化后端、公开注册或外部分析服务。
- 快捷指令如配置重试，应复用稳定的 `delivery_id`；缺省 ID 只适用于不重试的最小接入。

### 3.3 行为可解释

- 每次接收都应能用投递 ID 追踪；管理端记录不得把敏感正文写入普通日志。
- 服务端拒绝必须提供不泄露敏感数据的可执行原因，例如 Token 无效、字段缺失或请求过大。
- 发送端如实现自动重试，必须有次数上限、退避策略并复用投递 ID。

## 4. 实现边界

当前实现以 `PRD.md` 中的已交付范围为准，重点包括：

1. 由用户在 iOS 中明确创建短信触发的快捷指令自动化，并限制触发范围。
2. 快捷指令通过 HTTPS 向 `/api/webhooks/sms` 发送 Bearer 鉴权的 JSON POST 请求。
3. 服务端验证请求、幂等保存短信，并按接收号码、有效窗口和内置验证码规则路由。
4. 提供受保护的管理端、号池、密钥、使用记录与公开领取页。
5. 加密敏感数据，避免在日志、缓存及错误响应中泄露正文、号码或凭据。

除非用户明确要求，不应在首版加入以下内容：

- 原生 iOS 应用、读取 iOS 短信数据库或批量导入历史短信；系统快捷指令自动化接入属于当前支持范围。
- 尚未实现的 Android 原生接收端、设备端规则编辑、离线队列和重试调度。
- 第三方云端同步、额外控制台、公开注册或多租户账户体系。
- 邮件、Telegram、企业微信等额外目标渠道。
- 第三方短信存档、外部全文索引或分析服务；自托管后台的加密存储和受认证搜索属于当前功能。
- 远程读取设备历史短信。

## 5. 建议架构约束

具体技术栈确定后再补充模块和命令。实现时至少保持以下职责边界：

- `message-source`：当前由 iOS 快捷指令提供新短信正文和接收号码；未来平台客户端应转换为同一 Webhook 模型。
- `ingest`：验证 Bearer Token、请求大小、字段和幂等 ID，不负责公开页面呈现。
- `routing`：按接收号码、任务窗口和验证码规则匹配，不负责 HTTP 鉴权。
- `storage`：加密保存短信、号码、密钥和任务状态，并执行版本迁移。
- `ui`：管理端和公开领取页；不得将业务规则只写在界面代码中。

业务逻辑不得直接依赖界面组件。平台 API、时间、网络和持久化应通过可替换边界访问，以便单元测试。

## 6. 数据与接口约定

- 时间统一以 UTC 存储，界面按设备时区展示。
- 内部模型应有稳定 ID；不得用短信正文或手机号拼接生成 ID。
- Webhook 请求必须设置明确的 `Content-Type`、连接超时和读取超时。
- 使用稳定的投递 ID 支持幂等；自动重试不得创建新的投递 ID。
- iOS 快捷指令最小请求体为 `message` 和 `recipient`；可选字段必须保持向后兼容，不能要求快捷指令发送服务端可安全生成的字段。
- 服务端 `2xx` 视为成功；其他状态按 PRD 规定分类处理。
- 对外数据结构一旦发布，变更时必须考虑向后兼容或增加版本字段。

## 7. 编码规范

- 遵循所选语言的官方格式化工具和仓库已有风格。
- 保持函数职责单一，优先使用清晰命名而非解释性注释。
- 只在行为不直观、存在平台限制或安全权衡时添加简短注释。
- 不提交死代码、调试开关、真实手机号、真实短信或有效凭据。
- 新增依赖前检查许可证、维护状态、包体影响和最小系统版本兼容性。
- 不进行与当前任务无关的大规模重构或格式化。

## 8. 测试要求

每项行为变更都应提供与风险匹配的测试。至少覆盖：

- 仅带 `message`、`recipient` 的 iOS 快捷指令最小请求可以成功接收。
- 缺少必填字段、无效或格式错误的 Bearer Token、超限请求体会被拒绝。
- 相同 `delivery_id` 幂等去重，缺省 ID、发送方和 UTC 时间按接口约定生成。
- 接收号码、任务时间窗和验证码规则的命中与不命中。
- 手机号标准化、加密存储及管理端/公开端不同的输出边界。
- 敏感数据不会进入普通日志或诊断信息。

iOS 快捷指令界面和系统自动化行为需在真实设备上手工验证，并在 `README.md` 记录步骤；可自动化的服务端契约仍必须由测试覆盖。未来涉及 Android 系统广播、后台限制或进程重启的功能，除单元测试外还应有设备或模拟器集成测试。

## 9. 文档与变更要求

- 功能范围、用户流程或验收标准变化时同步更新 `PRD.md`。
- 技术栈确定后，在本文补充真实的环境要求、目录结构、构建及测试命令。
- 新增配置项时记录用途、默认值、是否敏感以及迁移策略。
- 数据格式、权限或安全策略发生变化时，在变更说明中单独列出。

## 10. 交付检查清单

提交工作前确认：

- 变更只覆盖任务所需范围。
- 格式化、静态检查和相关测试均已执行并通过。
- 没有提交密钥、个人数据、构建产物或本地环境文件。
- 日志和错误提示不泄露短信正文及完整手机号。
- 新行为符合 `PRD.md`，并有相应测试或明确说明未测试原因。
- 用户可见文案、权限说明和失败处理完整。
